# Mahrukh Portfolio

Frontend-only React/Vite portfolio site.

## Run locally

1. Open the project folder.
2. Install dependencies:
   - `npm install`
3. Start development server:
   - `npm run dev`

## Build for production

- `npm run build`
- `npm run preview`
